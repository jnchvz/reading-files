// Utiliser une classe abstraite principale pour définir les méthodes qui ne changeront pas d'un fichier  à l'autre
public abstract class AbstractReadFile {
	abstract void open();
	abstract void read();
}
